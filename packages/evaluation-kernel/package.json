{
	"name": "@earendil-works/evaluation-kernel",
	"version": "0.80.10",
	"description": "Trusted Evaluation Kernel (TEK) — private Phase 0a skeleton package. Never published, never imported by workspace packages.",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "vitest --run",
		"start": "tsx src/main.ts"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"license": "MIT"
}
